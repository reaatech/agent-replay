import type { Trace } from '@reaatech/shared';

export interface TraceViewerProps {
  trace: Trace;
}

// TODO: Implement React-based trace viewer component
// This will provide a visual timeline of spans, checkpoints, and events

export function createTraceViewer(_props: TraceViewerProps): unknown {
  // Placeholder: Will return a React component or DOM structure
  throw new Error('Web UI not yet implemented');
}
