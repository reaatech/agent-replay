{
  "name": "@reaatech/cli",
  "version": "0.1.0",
  "description": "Command-line interface for Agent Replay",
  "license": "MIT",
  "author": "Rick Somers <rick@reaatech.com> (https://reaatech.com)",
  "homepage": "https://github.com/reaatech/agent-replay#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reaatech/agent-replay.git",
    "directory": "packages/cli"
  },
  "bugs": {
    "url": "https://github.com/reaatech/agent-replay/issues"
  },
  "keywords": [
    "agent",
    "replay",
    "llm",
    "cli",
    "debugging"
  ],
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "main": "./dist/index.js",
  "bin": {
    "agent-replay": "./dist/cli.js"
  },
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts src/cli.ts --format esm --dts",
    "dev": "tsup src/index.ts src/cli.ts --format esm --dts --watch",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "prepublishOnly": "pnpm run build && pnpm test"
  },
  "dependencies": {
    "@reaatech/shared": "workspace:*",
    "@reaatech/core": "workspace:*",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  }
}
